%h1= @doc.title
%p= @doc.content

= link_to "All Docs", docs_path   #showing all the doc
= link_to "Fix Doc", edit_doc_path(@doc)  #select and edit the doc
= link_to "Trash It", doc_path(@doc), method: :delete, data: { confirm: "Are you sure?" }  # delete the doc and then showing.